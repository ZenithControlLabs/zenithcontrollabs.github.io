/*jshint esversion: 6 */
// @ts-check

/*
async function setZLBind(thing) {
    console.log(Number(thing.value));
    await device.sendFeatureReport(CmdBindZL, new Uint8Array([Number(thing.value)]));
}

async function setZRBind(thing) {
    await device.sendFeatureReport(CmdBindZR, new Uint8Array([Number(thing.value)]));
}*/